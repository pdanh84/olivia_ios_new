import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'olivia_ios_new',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
